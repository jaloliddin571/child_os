import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF1A6BFF);
  static const primaryDark = Color(0xFF0A4FCC);
  static const primaryLight = Color(0xFFE8F0FF);

  // Background
  static const bgDark = Color(0xFF0A0E1A);
  static const surface = Color(0xFF131929);
  static const card = Color(0xFF1C2540);
  static const border = Color(0x14FFFFFF); // 8% white

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8A94B0);
  static const textMuted = Color(0xFF4A5270);

  // Semantic
  static const success = Color(0xFF22C87A);
  static const warning = Color(0xFFFFB020);
  static const error = Color(0xFFFF5252);
  static const purple = Color(0xFF9B6FFF);

  // XP / Gamification
  static const xpGold = Color(0xFFFFD700);
  static const xpBlue = Color(0xFF1A6BFF);
}